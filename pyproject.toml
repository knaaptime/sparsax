[build-system]
requires = ["scikit-build-core>=0.10", "nanobind>=2.0", "jax>=0.9.0"]
build-backend = "scikit_build_core.build"

[project]
name = "sparsax"
version = "0.8.0"
description = "JAX-native sparse direct solvers via SuiteSparse (CHOLMOD + KLU)"
readme = "README.md"
license = {text = "BSD-3"}
requires-python = ">=3.9"
dependencies = [
    "jax>=0.9.0",
    "numpy",
]

[project.optional-dependencies]
test = ["pytest"]
# Optional PyTensor frontend (sparsax.pytensor) for PyMC's default backend,
# including gradient-based samplers (NUTS). The base package stays JAX-only.
pytensor = ["pytensor>=2.18"]

[tool.scikit-build]
cmake.build-type = "Release"
wheel.packages = ["src/sparsax"]
